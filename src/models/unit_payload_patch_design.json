{
    "endpoint": "PATCH /units/payload",
    "description": "Batch partial update of payloads (merges into existing JSONB)",
    "request_body": [
        {
            "id": "Hash32 (required)",
            "payload": "object (merges with current)"
        }
    ],
    "response": {
        "status": 200,
        "body": { "updated": "integer" }
    }
}