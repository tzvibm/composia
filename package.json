{
  "name": "composia",
  "version": "2.0.0",
  "type": "module",
  "description": "Graph-backed knowledge base for agents. Like Obsidian, but with a real graph database.",
  "main": "src/index.js",
  "bin": {
    "composia": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js serve",
    "dev": "node src/cli.js serve",
    "bench": "node src/benchmark.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "classic-level": "^2.0.0",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
